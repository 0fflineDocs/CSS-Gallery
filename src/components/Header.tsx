export function Header() {
  return (
    <header style={{ marginTop: '20px', marginBottom: '64px' }}>
      <h1
        className="text-4xl sm:text-5xl font-bold tracking-tight bg-gradient-to-r from-white to-slate-400 bg-clip-text text-transparent"
        style={{ fontFamily: "'Syne', sans-serif" }}
      >
        Theme Presets
      </h1>
    </header>
  );
}
